# /home/pi/pi-offloader/.env
# Environment variables for the SDTransfer Offloader
# !!! EDIT THIS FILE ON THE PI AFTER CLONING - DO NOT COMMIT REAL SECRETS TO GITHUB !!!

# --- Core Settings ---
# Generate a strong, random secret key for Flask session security.
FLASK_SECRET_KEY=replace_with_your_strong_random_flask_secret_key

# Admin credentials for the web UI (leave blank initially for first setup on Pi)
ADMIN_USERNAME=
ADMIN_PASSWORD=

# --- Notification Settings ---
# Generate a secure random token for internal script notifications
INTERNAL_NOTIFY_TOKEN=replace_with_your_generated_secure_random_notify_token

# --- Path Settings ---
# Mount point for the SD card (VERIFY ON YOUR PI!)
SD_MOUNT_PATH=/media/pi/SDCARD

# Local storage for copied files
LOCAL_FOOTAGE_PATH=/home/pi/footage

# Rclone config file location (default is usually fine)
RCLONE_CONFIG_PATH=/home/pi/.config/rclone/rclone.conf

# Log file locations
UPLOAD_LOG=/home/pi/pi-offloader/logs/rclone.log
OFFLOAD_LOG=/home/pi/pi-offloader/logs/offload.log

# Email config location
EMAIL_CONFIG_PATH=/home/pi/pi-offloader/email_config.json

# Disk path to monitor in UI
MONITORED_DISK_PATH=/home/pi/footage

# Config backup location
CONFIG_BACKUP_PATH=/home/pi/config_backups

# --- Rclone Settings ---
# Name of your configured rclone remote
RCLONE_REMOTE_NAME=gdrive

# Base path on Google Drive
RCLONE_REMOTE_BASE_PATH=FX3_Backups

# Flags for rclone copy/copyto commands (add --log-file if needed per-command)
RCLONE_COPY_FLAGS="--min-age 1m --contimeout 60s --timeout 300s --retries 3"

# --- SD Card Specific Paths (Relative to SD_MOUNT_PATH - CHECK YOUR CARD!) ---
VIDEO_SUBDIR=PRIVATE/M4ROOT/CLIP
PHOTO_SUBDIR=DCIM/100MSDCF