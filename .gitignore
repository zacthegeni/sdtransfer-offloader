# Git Ignore file for SDTransfer Offloader

# Environment variables (contains secrets!)
.env

# Python virtual environment
venv/

# Python bytecode and caches
__pycache__/
*.py[cod]
*$py.class

# OS generated files
.DS_Store
Thumbs.db

# Log files
logs/
*.log

# Configuration backups
config_backups/

# Local footage (potentially large)
footage/

# Rclone config file (potentially contains tokens)
# If RCLONE_CONFIG_PATH points outside the project dir, this isn't strictly needed here,
# but good practice if it *could* be inside. Check your .env setting.
# /path/to/.config/rclone/rclone.conf
# Example if it was inside the project (don't uncomment unless it IS):
# rclone.conf

# Gunicorn socket file
*.sock

# Email config (contains password)
email_config.json

# Marker files generated by scripts
# (These live alongside media in footage/, which is ignored anyway)
*.copied
*.uploaded